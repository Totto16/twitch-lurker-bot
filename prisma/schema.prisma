generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Message {
    id      BigInt   @id @default(autoincrement()) @db.BigInt
    message String
    channel String
    name    String
    user_id String
    time    DateTime
}
